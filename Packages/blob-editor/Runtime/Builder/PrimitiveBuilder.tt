<#@ template language="C#" #>
<#@ output extension=".cs" #>

<#@ include file="GenerateBuilders.ttinclude" #>

namespace Blob.Primitive
{

<#
    foreach (var type in typeof(int).Assembly.GetTypes())
    {
        if (type.IsPrimitive)
        {
            GenerateBuilders(type);
        }
    }
#>

}